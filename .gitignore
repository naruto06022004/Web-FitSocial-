# === BMAD-METHOD (local install) ===
/_bmad/
/_bmad-output/

# Some installs/tools may create similarly-named folders
/_bmad-*/

# === Cursor (local project data / downloaded skills) ===
/.cursor/

# === Local AI tool folders (optional) ===
/.claude/
/.ai/

# === Toolchains / SDKs (local) ===
/.tools/

# === Secrets / local keys (never commit) ===
/key-pem/
*.pem

# === Local env / credentials ===
.env
.env.*
!.env.example

# === Flutter/Dart tooling (local) ===
/frontend/devtools_options.yaml

